---
layout: post
title: ZGC는 어떻게 거의 멈추지 않고 힙을 정리할까
description: >
  ZGC의 colored pointer, load barrier, forwarding table, concurrent relocation, Generational ZGC를 정리합니다.
tags: [java, gc, jvm, zgc]
sitemap: false
---

# ZGC는 어떻게 거의 멈추지 않고 힙을 정리할까

G1은 멈춤 시간을 예측 가능하게 줄인다. 하지만 객체를 실제로 옮기는 evacuation은 여전히 STW 안에서 처리한다. 힙이 커지고 살아있는 객체가 많아질수록, 이 비용은 무시하기 어려워진다.

ZGC의 질문은 조금 더 과감하다.

> 객체를 옮기는 동안에도 애플리케이션을 계속 실행할 수는 없을까?

ZGC는 이 질문에 `colored pointer`와 `load barrier`로 답한다. 참조 자체에 작은 상태 비트를 붙이고, 객체를 읽는 순간 포인터가 현재 GC 상태에 맞는지 검사한다. 낡은 주소를 만나면 그 자리에서 고친다.

<style>
.gc-zgc-story {
  --zgc-cycle: 9s;
  --zgc-panel: rgba(255, 250, 242, .075);
  --zgc-line: rgba(255, 250, 242, .18);
}

.gc-zgc-story .zgc-story-flow {
  display: grid;
  gap: .8rem;
}

.gc-zgc-story .zgc-story-guide {
  display: grid;
  grid-template-columns: repeat(4, minmax(0, 1fr));
  gap: .45rem;
  margin-bottom: .1rem;
}

.gc-zgc-story .zgc-story-guide span {
  min-width: 0;
  padding: .48rem .55rem;
  border: 1px solid rgba(255, 250, 242, .14);
  border-radius: 6px;
  background: rgba(255, 250, 242, .055);
}

.gc-zgc-story .zgc-story-guide b,
.gc-zgc-story .zgc-story-guide em {
  display: block;
}

.gc-zgc-story .zgc-story-guide b {
  color: #fffaf2;
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: .68rem;
}

.gc-zgc-story .zgc-story-guide em {
  margin-top: .12rem;
  color: rgba(255, 250, 242, .66);
  font-size: .62rem;
  font-style: normal;
  line-height: 1.45;
}

.gc-zgc-story .zgc-story-top,
.gc-zgc-story .zgc-story-diagram,
.gc-zgc-story .zgc-story-steps {
  display: grid;
  gap: .65rem;
}

.gc-zgc-story .zgc-story-top {
  grid-template-columns: 1fr 1.05fr;
}

.gc-zgc-story .zgc-story-card,
.gc-zgc-story .zgc-story-panel,
.gc-zgc-story .zgc-story-lane,
.gc-zgc-story .zgc-story-steps li {
  border: 1px solid var(--zgc-line);
  border-radius: 6px;
  background: var(--zgc-panel);
}

.gc-zgc-story .zgc-story-card {
  min-width: 0;
  padding: .72rem .8rem;
}

.gc-zgc-story .zgc-story-label {
  display: block;
  color: rgba(255, 250, 242, .62);
  font-size: .68rem;
}

.gc-zgc-story .zgc-story-field {
  display: grid;
  grid-template-columns: auto minmax(0, 1fr);
  gap: .6rem;
  align-items: center;
  margin-top: .42rem;
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
}

.gc-zgc-story .zgc-story-field strong {
  color: #fffaf2;
  font-size: .82rem;
  font-weight: 700;
}

.gc-zgc-story .zgc-story-field-value {
  position: relative;
  min-height: 1.8rem;
  border-radius: 6px;
  background: rgba(8, 10, 17, .34);
}

.gc-zgc-story .zgc-story-field-value code {
  position: absolute;
  inset: 0;
  display: flex;
  align-items: center;
  padding: 0 .55rem;
  color: #fffaf2;
  background: transparent;
  font-size: .78rem;
}

.gc-zgc-story .zgc-story-field-old {
  animation: zgc-story-field-old var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-field-new {
  animation: zgc-story-field-new var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-note {
  margin: .38rem 0 0;
  color: rgba(255, 250, 242, .72);
  font-size: .72rem;
  line-height: 1.55;
}

.gc-zgc-story .zgc-story-bits {
  display: grid;
  grid-template-columns: 1.2fr repeat(3, .72fr);
  gap: .35rem;
  margin-top: .55rem;
}

.gc-zgc-story .zgc-story-bits span {
  min-width: 0;
  padding: .34rem .42rem;
  border: 1px solid rgba(255, 250, 242, .14);
  border-radius: 6px;
  color: rgba(255, 250, 242, .78);
  background: rgba(255, 250, 242, .06);
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: .66rem;
  text-align: center;
}

.gc-zgc-story .zgc-story-bits .zgc-story-remap-bit {
  animation: zgc-story-remap-bit var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-diagram {
  grid-template-columns: minmax(0, 1fr) minmax(8rem, .85fr) minmax(0, 1fr);
  align-items: stretch;
}

.gc-zgc-story .zgc-story-panel {
  position: relative;
  min-height: 8.2rem;
  overflow: hidden;
  padding: .68rem;
}

.gc-zgc-story .zgc-story-panel-title {
  color: rgba(255, 250, 242, .68);
  font-size: .68rem;
}

.gc-zgc-story .zgc-story-object {
  position: absolute;
  left: 50%;
  top: 48%;
  width: .85rem;
  height: .85rem;
  border-radius: 50%;
  transform: translate(-50%, -50%);
}

.gc-zgc-story .zgc-story-object::after {
  position: absolute;
  left: 1rem;
  top: -.1rem;
  color: rgba(255, 250, 242, .78);
  font-size: .68rem;
  white-space: nowrap;
}

.gc-zgc-story .zgc-story-object-old {
  background: #d98989;
  box-shadow: 0 0 .85rem rgba(217, 137, 137, .72);
  animation: zgc-story-old-object var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-object-old::after {
  content: "B_old";
}

.gc-zgc-story .zgc-story-object-new {
  background: #8fbfa0;
  box-shadow: 0 0 .85rem rgba(143, 191, 160, .72);
  animation: zgc-story-new-object var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-object-new::after {
  content: "B_new";
}

.gc-zgc-story .zgc-story-table {
  display: grid;
  align-content: center;
  gap: .45rem;
}

.gc-zgc-story .zgc-story-table-row {
  padding: .5rem .55rem;
  border: 1px solid rgba(255, 250, 242, .16);
  border-radius: 6px;
  color: #fffaf2;
  background: rgba(8, 10, 17, .3);
  font-family: ui-monospace, SFMono-Regular, Menlo, Consolas, monospace;
  font-size: .72rem;
  animation: zgc-story-table-row var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-lane {
  position: relative;
  min-height: 5.05rem;
  padding: .78rem .75rem .7rem;
}

.gc-zgc-story .zgc-story-lane::before {
  content: "load barrier";
  position: absolute;
  left: .75rem;
  top: .48rem;
  color: rgba(255, 250, 242, .66);
  font-size: .64rem;
}

.gc-zgc-story .zgc-story-lane-line {
  position: absolute;
  left: 1.2rem;
  right: 1.2rem;
  top: 2rem;
  height: .12rem;
  border-radius: 999px;
  background: linear-gradient(90deg, rgba(217, 137, 137, .38), rgba(185, 165, 233, .68), rgba(143, 191, 160, .42));
}

.gc-zgc-story .zgc-story-pulse {
  position: absolute;
  z-index: 1;
  left: 12%;
  top: 2rem;
  width: .72rem;
  height: .72rem;
  border-radius: 50%;
  background: #fffaf2;
  box-shadow: 0 0 .8rem rgba(255, 250, 242, .92), 0 0 1.3rem rgba(185, 165, 233, .62);
  animation: zgc-story-pulse var(--zgc-cycle) ease-in-out infinite;
}

.gc-zgc-story .zgc-story-pulse b {
  position: absolute;
  left: 50%;
  top: -1.4rem;
  padding: .16rem .38rem;
  border: 1px solid rgba(255, 250, 242, .16);
  border-radius: 999px;
  color: rgba(255, 250, 242, .72);
  background: rgba(8, 10, 17, .54);
  font-size: .62rem;
  font-weight: 400;
  white-space: nowrap;
  transform: translateX(-50%);
}

.gc-zgc-story .zgc-story-lane-labels {
  display: grid;
  grid-template-columns: repeat(3, minmax(0, 1fr));
  gap: .45rem;
  padding-top: 2.55rem;
}

.gc-zgc-story .zgc-story-lane-labels span {
  color: rgba(255, 250, 242, .7);
  font-size: .66rem;
  text-align: center;
}

.gc-zgc-story .zgc-story-steps {
  grid-template-columns: repeat(4, minmax(0, 1fr));
  margin: 0;
  padding: 0;
  list-style: none;
}

.gc-zgc-story .zgc-story-steps li {
  min-width: 0;
  padding: .55rem .6rem;
  animation-duration: var(--zgc-cycle);
  animation-iteration-count: infinite;
  animation-timing-function: ease-in-out;
}

.gc-zgc-story .zgc-story-steps li:nth-child(1) { animation-name: zgc-story-step-1; }
.gc-zgc-story .zgc-story-steps li:nth-child(2) { animation-name: zgc-story-step-2; }
.gc-zgc-story .zgc-story-steps li:nth-child(3) { animation-name: zgc-story-step-3; }
.gc-zgc-story .zgc-story-steps li:nth-child(4) { animation-name: zgc-story-step-4; }

.gc-zgc-story .zgc-story-steps strong,
.gc-zgc-story .zgc-story-steps span {
  display: block;
}

.gc-zgc-story .zgc-story-steps strong {
  color: #fffaf2;
  font-size: .74rem;
}

.gc-zgc-story .zgc-story-steps span {
  margin-top: .14rem;
  color: rgba(255, 250, 242, .68);
  font-size: .66rem;
  line-height: 1.45;
}

.zgc-cycle-map {
  margin: 1.2rem 0 1.4rem;
}

.zgc-cycle-map ol {
  display: grid;
  grid-template-columns: repeat(7, minmax(0, 1fr));
  gap: .4rem;
  margin: 0;
  padding: 0;
  list-style: none;
}

.zgc-cycle-map li {
  min-width: 0;
  padding: .58rem .5rem;
  border: 1px solid rgba(255, 250, 242, .15);
  border-radius: 6px;
  background: rgba(255, 250, 242, .06);
}

.zgc-cycle-map li[data-kind="pause"] {
  border-color: rgba(217, 137, 137, .32);
  background: rgba(217, 137, 137, .13);
}

.zgc-cycle-map li[data-kind="concurrent"] {
  border-color: rgba(143, 191, 160, .26);
  background: rgba(143, 191, 160, .1);
}

.zgc-cycle-map b,
.zgc-cycle-map span {
  display: block;
}

.zgc-cycle-map b {
  color: #fffaf2;
  font-size: .68rem;
  line-height: 1.3;
}

.zgc-cycle-map span {
  margin-top: .18rem;
  color: rgba(255, 250, 242, .66);
  font-size: .62rem;
  line-height: 1.45;
}

@keyframes zgc-story-field-old {
  0%, 66% { opacity: 1; transform: translateY(0); }
  74%, 100% { opacity: 0; transform: translateY(-.24rem); }
}

@keyframes zgc-story-field-new {
  0%, 68% { opacity: 0; transform: translateY(.24rem); }
  78%, 100% { opacity: 1; transform: translateY(0); }
}

@keyframes zgc-story-remap-bit {
  0%, 22% { background: rgba(217, 137, 137, .2); box-shadow: 0 0 .7rem rgba(217, 137, 137, .18); }
  32%, 66% { background: rgba(185, 165, 233, .22); box-shadow: 0 0 .7rem rgba(185, 165, 233, .22); }
  78%, 100% { background: rgba(143, 191, 160, .22); box-shadow: 0 0 .7rem rgba(143, 191, 160, .2); }
}

@keyframes zgc-story-old-object {
  0%, 44% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
  58%, 100% { opacity: .4; transform: translate(-50%, -50%) scale(.84); }
}

@keyframes zgc-story-new-object {
  0%, 30% { opacity: .26; transform: translate(-50%, -50%) scale(.78); }
  46%, 100% { opacity: 1; transform: translate(-50%, -50%) scale(1); }
}

@keyframes zgc-story-table-row {
  0%, 42%, 70%, 100% { background: rgba(8, 10, 17, .3); box-shadow: none; }
  50%, 64% { background: rgba(185, 165, 233, .2); box-shadow: 0 0 1rem rgba(185, 165, 233, .2); }
}

@keyframes zgc-story-pulse {
  0%, 28% { left: 12%; opacity: 1; transform: translate(-50%, -50%) scale(1); }
  50%, 66% { left: 50%; opacity: 1; transform: translate(-50%, -50%) scale(1.08); }
  76%, 100% { left: 88%; opacity: 1; transform: translate(-50%, -50%) scale(1); }
}

@keyframes zgc-story-step-1 {
  0%, 24% { background: rgba(217, 137, 137, .2); box-shadow: 0 0 .9rem rgba(217, 137, 137, .18); }
  32%, 100% { background: var(--zgc-panel); box-shadow: none; }
}

@keyframes zgc-story-step-2 {
  0%, 24%, 52%, 100% { background: var(--zgc-panel); box-shadow: none; }
  32%, 46% { background: rgba(142, 182, 217, .18); box-shadow: 0 0 .9rem rgba(142, 182, 217, .18); }
}

@keyframes zgc-story-step-3 {
  0%, 46%, 72%, 100% { background: var(--zgc-panel); box-shadow: none; }
  54%, 66% { background: rgba(185, 165, 233, .2); box-shadow: 0 0 .9rem rgba(185, 165, 233, .18); }
}

@keyframes zgc-story-step-4 {
  0%, 66% { background: var(--zgc-panel); box-shadow: none; }
  76%, 100% { background: rgba(143, 191, 160, .2); box-shadow: 0 0 .9rem rgba(143, 191, 160, .18); }
}

@media screen and (max-width: 56rem) {
  .gc-zgc-story .zgc-story-guide {
    grid-template-columns: 1fr 1fr;
  }

  .gc-zgc-story .zgc-story-top,
  .gc-zgc-story .zgc-story-diagram,
  .gc-zgc-story .zgc-story-steps {
    grid-template-columns: 1fr;
  }

  .gc-zgc-story .zgc-story-bits {
    grid-template-columns: 1fr 1fr;
  }

  .gc-zgc-story .zgc-story-lane {
    min-height: 5.45rem;
  }

  .zgc-cycle-map ol {
    grid-template-columns: 1fr;
  }
}
</style>

<div class="gc-visual gc-zgc-story" role="img" aria-label="ZGC relocation에서 B_old가 B_new로 복사되고, load barrier가 A.child의 낡은 참조를 forwarding table로 고치는 흐름">
  <div class="gc-visual__header">
    <strong>ZGC relocation 흐름</strong>
    <span>핵심은 객체가 움직이는 장면보다, 낡은 참조 <code>A.child -> B_old</code>가 읽히는 순간 <code>A.child -> B_new</code>로 고쳐지는 장면이다.</span>
  </div>
  <div class="zgc-story-flow" aria-hidden="true">
    <div class="zgc-story-guide">
      <span><b>B_old</b><em>옮기기 전 주소</em></span>
      <span><b>B_new</b><em>새 page에 복사된 주소</em></span>
      <span><b>table</b><em>옛 주소와 새 주소의 매핑</em></span>
      <span><b>self-heal</b><em>읽은 김에 참조를 새 주소로 저장</em></span>
    </div>

    <div class="zgc-story-top">
      <div class="zgc-story-card">
        <span class="zgc-story-label">애플리케이션 필드 슬롯</span>
        <div class="zgc-story-field">
          <strong>A.child</strong>
          <span class="zgc-story-field-value">
            <code class="zgc-story-field-old">-> B_old</code>
            <code class="zgc-story-field-new">-> B_new</code>
          </span>
        </div>
      </div>
      <div class="zgc-story-card">
        <span class="zgc-story-label">colored pointer 상태</span>
        <p class="zgc-story-note">remap bit가 낡으면, 읽기 시점의 load barrier가 forwarding table을 확인한다.</p>
        <div class="zgc-story-bits">
          <span>address</span>
          <span>marked</span>
          <span class="zgc-story-remap-bit">remap</span>
          <span>remember</span>
        </div>
      </div>
    </div>

    <div class="zgc-story-diagram">
      <div class="zgc-story-panel">
        <span class="zgc-story-panel-title">old page / relocation set</span>
        <i class="zgc-story-object zgc-story-object-old"></i>
      </div>
      <div class="zgc-story-panel zgc-story-table">
        <span class="zgc-story-panel-title">forwarding table</span>
        <span class="zgc-story-table-row">B_old -> B_new</span>
      </div>
      <div class="zgc-story-panel">
        <span class="zgc-story-panel-title">new page / to-space</span>
        <i class="zgc-story-object zgc-story-object-new"></i>
      </div>
    </div>

    <div class="zgc-story-lane">
      <span class="zgc-story-lane-line"></span>
      <i class="zgc-story-pulse"><b>load barrier</b></i>
      <div class="zgc-story-lane-labels">
        <span>1. A.child reads B_old</span>
        <span>2. lookup B_old -> B_new</span>
        <span>3. write back B_new</span>
      </div>
    </div>

    <ol class="zgc-story-steps">
      <li><strong>1. 낡은 참조</strong><span>A.child 값은 아직 B_old다.</span></li>
      <li><strong>2. 객체 복사</strong><span>GC가 B_new를 만들어 둔다.</span></li>
      <li><strong>3. table 조회</strong><span>B_old -> B_new 매핑을 찾는다.</span></li>
      <li><strong>4. self-heal</strong><span>A.child 값을 B_new로 고쳐 저장한다.</span></li>
    </ol>
  </div>
</div>

## ZGC가 풀려는 문제

GC가 해야 할 일은 결국 두 가지다.

```text
1. 어떤 객체가 살아 있는지 찾는다.
2. 죽은 객체가 차지하던 메모리를 다시 쓸 수 있게 만든다.
```

문제는 애플리케이션도 동시에 객체 그래프를 바꾼다는 점이다. 참조가 생기고 끊기고, 어떤 객체는 방금까지 살아 있다가 바로 죽는다. 가장 쉬운 해결책은 애플리케이션을 멈춘 뒤 한 번에 검사하는 것이다. 하지만 힙이 커질수록 pause가 길어진다.

ZGC는 STW를 매우 짧은 동기화 지점으로 제한하고, 마킹과 재배치 대부분을 애플리케이션과 동시에 수행한다. 그래서 목표는 처리량 최대화가 아니라 극단적으로 낮은 pause time이다.

## Colored Pointer: 포인터에 상태를 담는다

64비트 환경에서는 실제 객체 주소를 표현하는 데 모든 비트를 다 쓰지 않는 경우가 많다. ZGC는 이 여유 비트 일부를 GC 메타데이터로 사용한다. 이를 `colored pointer`라고 부른다.

개념적으로 보면 다음과 같다.

```text
[ address bits ][ metadata bits ]
                 ├─ marked
                 ├─ remapped
                 └─ remembered / finalizable 등
```

여기서 주의할 점이 있다. 색은 이전 주소나 새 주소를 뜻하지 않는다. 색은 이 포인터를 읽을 때 어떤 검사가 필요한지 알려주는 상태값이다.

예를 들어 marking 관련 bit는 이 객체가 이번 GC cycle에서 live로 확인되었는지와 관련된다. remap 관련 bit는 이 포인터의 주소가 relocation 이후에도 현재 주소라고 확인되었는지와 관련된다.

두 상태는 독립적이다.

```text
marked?   -> 이번 cycle에서 살아 있음이 확인되었는가
remapped? -> relocation 이후 주소가 최신임이 확인되었는가
```

객체가 살아 있다는 사실과, 그 객체를 가리키는 포인터가 최신 주소를 담고 있다는 사실은 같은 말이 아니다. ZGC가 metadata를 여러 축으로 나누는 이유가 여기에 있다.

## Load Barrier: 읽는 순간 포인터를 고친다

ZGC의 핵심은 load barrier다. 자바 코드에서는 단순히 필드를 읽는 것처럼 보인다.

```java
Object child = node.child;
```

하지만 JIT가 만든 실제 코드에는 참조를 읽은 직후 검사하는 경로가 들어간다.

```text
ref = node.child

if ref의 metadata가 현재 GC 상태와 맞지 않으면:
    ref = slow_path(ref)
    node.child = ref

return ref
```

대부분의 경우는 fast path다. 포인터 색이 현재 상태와 맞으면 거의 비용 없이 지나간다. 하지만 stale pointer를 만나면 slow path로 들어간다.

slow path는 다음 일을 할 수 있다.

- 아직 marking되지 않은 객체를 marking한다.
- relocation 대상 객체인지 확인한다.
- 옛 주소라면 forwarding table에서 새 주소를 찾는다.
- 필드 안의 참조를 새 주소와 현재 metadata로 다시 써서 self-healing한다.

이 self-healing 성질이 중요하다. 한 번 읽으며 고친 포인터는 다음에 다시 읽을 때 fast path로 지나간다. 시간이 지나면서 낡은 포인터는 자연스럽게 현재 GC cycle의 색으로 수렴한다.

## 이전 주소와 새 주소는 어디에 있나

ZGC에서 `old address -> new address` 매핑은 colored pointer 안에 들어 있지 않다. 포인터 안에는 주소 비트와 상태 비트가 있을 뿐이다.

객체가 relocation set에 들어가면 ZGC는 live object를 새 page로 복사하고, 별도의 forwarding table에 매핑을 기록한다.

```text
Heap
  A.child -> B_old   // stale pointer일 수 있음

Forwarding Table
  B_old -> B_new
```

애플리케이션이 `A.child`를 읽는 순간 load barrier가 현재 remap 상태와 맞지 않는 포인터를 발견한다. 그 주소가 relocation 대상이면 forwarding table을 조회해서 `B_new`를 얻고, `A.child`를 새 주소로 고친다.

```text
읽기 전
A.child = [B_old + old remap metadata]

load barrier
B_old가 relocation set인가?
  yes -> forwarding table에서 B_new 조회

읽기 후
A.child = [B_new + current remap metadata]
```

G1과의 차이는 뚜렷하다. G1은 STW evacuation 안에서 참조 slot을 새 주소로 고쳐 두어야 한다. ZGC는 주소 갱신 작업을 읽기 시점으로 흩어 놓는다. 그래서 relocation을 애플리케이션과 동시에 진행할 수 있다.

## ZGC의 GC cycle

ZGC cycle은 크게 marking과 relocation으로 나눌 수 있다. 각 단계의 시작과 끝에는 짧은 STW가 있지만, 오래 걸리는 작업은 대부분 concurrent하게 진행된다.

<div class="gc-visual zgc-cycle-map" role="img" aria-label="ZGC cycle은 짧은 pause와 concurrent 작업이 번갈아 이어지는 흐름">
  <ol>
    <li data-kind="pause"><b>Pause Mark Start</b><span>root 기준점 고정</span></li>
    <li data-kind="concurrent"><b>Concurrent Mark</b><span>힙을 동시에 추적</span></li>
    <li data-kind="pause"><b>Pause Mark End</b><span>mark 결과 동기화</span></li>
    <li data-kind="concurrent"><b>Prepare</b><span>relocation set 선정</span></li>
    <li data-kind="pause"><b>Pause Relocate</b><span>root 참조 보정</span></li>
    <li data-kind="concurrent"><b>Relocate</b><span>객체 복사와 table 기록</span></li>
    <li data-kind="concurrent"><b>Remap</b><span>stale pointer 정리</span></li>
  </ol>
</div>

```text
Pause Mark Start        root marking 시작
Concurrent Mark         힙을 동시에 추적
Pause Mark End          marking 종료 동기화
Concurrent Prepare      relocation set 선정
Pause Relocate Start    root relocation 시작
Concurrent Relocate     객체 이동과 pointer remap
Concurrent Remap        남은 stale pointer 정리
```

`Pause Mark Start`와 `Pause Mark End`, `Pause Relocate Start`는 전체 세계를 오래 멈추는 구간이 아니라, GC cycle의 기준점을 맞추는 짧은 동기화 지점에 가깝다.

긴 작업은 애플리케이션이 계속 실행되는 동안 진행된다. GC 스레드가 객체를 옮기고, 애플리케이션 스레드는 낡은 포인터를 읽을 때 load barrier를 통해 새 주소로 고친다. 때로는 mutator가 relocation을 돕는 구조가 된다.

## Page 단위 힙 관리

ZGC는 힙을 page 단위로 관리한다. G1의 region과 비슷하게 보일 수 있지만, 크기와 역할이 다르다.

- Small page: 작은 객체를 담는다.
- Medium page: 중간 크기 객체를 담는다.
- Large page: 큰 객체를 담고, 보통 객체 하나가 page 하나를 차지한다.

큰 객체를 자주 옮기면 비용이 커진다. 그래서 ZGC는 page 단위의 회수와 relocation set 선택으로 비용을 조절한다. garbage가 많은 page를 고르고, live object만 새 page로 옮긴다.

## Generational ZGC: 젊은 객체를 더 자주 본다

초기 ZGC는 non-generational이었다. 매 cycle마다 힙 전체를 바라보는 구조라 단순했지만, 자바 객체 대부분이 빨리 죽는다는 경험칙을 충분히 활용하지 못했다.

JDK 21부터는 Generational ZGC가 도입되었다. Young과 Old를 나누고, 대부분의 수집을 Young에서 자주 처리한다. ZGC의 낮은 pause 철학은 유지하면서도, 전체 힙을 매번 같은 무게로 보지 않도록 바뀐 것이다.

Generational ZGC에서 기억할 부분은 RSet의 방향이다.

G1의 RSet은 target-side 성격이 강하다. collection set 안으로 들어오는 외부 card를 기억한다. 반면 Generational ZGC의 RSet은 source-side에 가깝다. old page 안에 있는 필드 주소 중 young을 가리킬 가능성이 있는 slot을 기록한다.

```text
G1
  target region/group RSet
  <- source card 후보

Generational ZGC
  source old page RSet
  <- old page 안의 field slot 후보
```

또 하나의 차이는 해상도다. G1은 기본적으로 512바이트 card 단위로 후보를 기록한다. ZGC는 old page별 bitmap에 potential object field address를 더 정밀하게 기록한다. 다만 ZGC도 그 slot이 반드시 young을 가리킨다고 확정하는 것은 아니다. young collection 때 현재 값을 다시 읽어 확인한다.

정밀한 기록은 비용이 있다. 그래서 Generational ZGC는 store barrier를 fast path, medium path, slow path로 나누고, 같은 cycle 안에서 같은 필드가 반복해서 느린 경로를 타지 않도록 act-once 성질을 활용한다.

## ZGC의 비용

ZGC가 pause를 줄이는 대신 공짜로 모든 것을 얻는 것은 아니다.

가장 큰 비용은 barrier다. 참조를 읽을 때마다 load barrier 검사가 들어간다. 대부분은 빠르게 끝나지만, 그래도 처리량에는 영향을 준다.

또한 colored pointer, forwarding table, page metadata, remembered set 같은 구조가 필요하다. 운영 환경에서는 낮은 pause time이 정말 필요한지, 처리량 손실과 CPU 여유가 감당 가능한지 함께 봐야 한다.

ZGC가 잘 맞는 경우는 대체로 분명하다.

- pause time SLO가 매우 엄격하다.
- p99, p999 tail latency가 중요하다.
- 힙이 크고 live set도 크다.
- 짧은 정지가 비즈니스 장애로 이어진다.

반대로 일반적인 API 서버처럼 수십에서 수백 ms 수준의 pause가 허용되고 처리량이 더 중요하다면, G1이 더 단순하고 좋은 선택일 수 있다.

## ZGC를 한 문장으로 정리하면

ZGC는 포인터에 GC 상태를 담고, load barrier로 읽는 순간 낡은 참조를 고친다. 이전 주소와 새 주소의 매핑은 별도 forwarding table에 두고, 객체 이동과 참조 갱신을 애플리케이션 실행 중에 나누어 처리한다.

G1이 region을 골라 멈춤 시간을 관리하는 collector라면, ZGC는 참조 자체를 GC 프로토콜에 참여시켜 멈춤 시간을 거의 없애려는 collector다. 그래서 ZGC의 핵심은 단순히 빠른 GC가 아니라, 애플리케이션과 GC가 동시에 같은 힙 위를 조심스럽게 걷도록 만드는 설계에 있다.
